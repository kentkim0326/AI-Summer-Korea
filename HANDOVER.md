# 인수인계 — AI Summer Korea 사이트

이 문서는 **다음 사람이 이 저장소를 처음 열었을 때** 필요한 것만 담았습니다.
프로그램 자체의 기획이 아니라 사이트를 고치고 배포하는 방법입니다.

작성 2026-08-24 · 마지막 배포 커밋은 `git log -1` 로 확인하세요.

---

## 1. 이게 뭔가

전세계 대학생을 대상으로 하는 4주 한국 AI·반도체 프로그램(2027년 여름 1기)의 소개 사이트입니다.

- **라이브** — https://korea-ai-summer.vercel.app/
- **저장소** — `kentkim0326/AI-Summer-Korea` (GitHub, 공개)
  2026-08-24 에 `korea-ai-summer` 에서 이름이 바뀌었습니다. GitHub 이 옛 주소를 리디렉션해 주지만
  영구 보장은 아니므로, 옛 주소로 클론해 둔 사본이 있으면 `git remote set-url` 로 갱신하세요.
- **배포** — GitHub 연동. `main` 에 푸시하면 Vercel 이 자동 배포합니다.
  현재 `main` 과 `claude/gamma-connection-check-rty3e7` 를 같은 커밋으로 유지하고 있습니다.
  어느 브랜치가 프로덕션인지 Vercel 설정에서 확실히 해두는 편이 좋습니다.

빌드 도구, 번들러, 프레임워크가 없습니다. 정적 파일 그대로 서빙됩니다.
`npm install` 할 것이 없고, 파일을 고쳐 푸시하면 그것이 배포입니다.

---

## 2. 파일 구조

```
index.html              페이지 전체. 영어 원문이 여기에 들어 있다.
styles.css              전부. 단일 다크 테마.
i18n.js                 8개 언어 번역 데이터 + 언어 목록.
app.js                  언어 전환, 리스트 렌더, 히어로 영상, 서비스워커 등록.
build-static.js         i18n.js → index.html 의 목록 마크업 동기화.
sw.js                   서비스워커 (오프라인 캐시).
manifest.webmanifest    PWA 매니페스트.
assets/                 로고, 파비콘, 히어로 영상·포스터, 섹션 밴드 이미지.
```

**영어 원문이 `index.html` 과 `i18n.js` 양쪽에 있습니다.** 의도된 중복입니다 —
자바스크립트를 실행하지 않는 크롤러(검색엔진, 메일 미리보기, 링크 카드)도
전체 문장을 읽을 수 있어야 하기 때문입니다.
**영어 문구를 고칠 때는 반드시 두 곳 다 고쳐야 합니다.**

---

## 3. 문구를 고치려면

### 영어

1. `index.html` 에서 해당 문장을 찾아 고칩니다.
2. `i18n.js` 의 `en:` 블록에서 같은 문장을 찾아 똑같이 고칩니다.
3. 목록류(방문지, 지원자격, FAQ, 후원 표)를 고쳤다면:

```bash
node build-static.js          # i18n.js 기준으로 index.html 목록을 다시 씀
node build-static.js --check  # 어긋나면 exit 1 (CI 에 걸어두면 좋음)
```

### 그 외 언어

`i18n.js` 의 해당 언어 블록만 고치면 됩니다. HTML 은 건드리지 않습니다.

---

## 4. 언어

현재 8개: `en` `ko` `zh-Hans` `zh-Hant` `ja` `es` `fr` `de`

**`en` 과 `ko` 만 전체 번역입니다.** 나머지 6개는 핵심 섹션만 번역했습니다 —
`nav` `hero` `why` `program` `apply` `vol` `faq` `foot`.
번역하지 않은 섹션(`visits` `spon` `forum` `about`)은 **영어로 표시됩니다.**

이건 버그가 아니라 결정입니다. 후원·파트너·법인 문구는 가장 자주 바뀌는 부분이고,
철 지난 번역 8개보다 정확한 영어 하나가 낫다고 판단했습니다.

폴백은 `app.js` 의 `pick()` 이 처리합니다. 키가 없으면 **영어**로 떨어집니다.
직전 언어의 텍스트가 남지 않습니다 — 한국어를 보다 프랑스어로 바꿔도
후원 섹션에 한국어가 남지 않는다는 뜻입니다. 이 동작을 깨뜨리지 마세요.

### 언어를 추가하려면

1. `i18n.js` 의 `LANGS` 배열에 항목 추가:
   `{ code:"pt", label:"Português", english:"Portuguese", tag:"pt", dir:"ltr" }`
2. `STATUS_LABELS` 에 `confirmed` / `talks` / `planned` 세 단어 추가.
3. `I18N` 에 해당 코드로 블록 추가. 기존 핵심 블록을 복사해 번역하세요.
4. **배열 길이를 반드시 맞추세요** — `apply.eligItems` 6개, `apply.coveredItems` 6개,
   `faq.items` 8개. 하나라도 모자라면 그 언어에서만 목록이 짧게 나오고
   아무 경고도 뜨지 않습니다.

### 언어 자동 감지

`app.js` 의 `detect()` 가 처리합니다. 주의할 점 하나 —
**`navigator.language` 를 두 글자로 자르면 안 됩니다.** `zh-TW` 와 `zh-CN` 이
같아져서 대만 사용자에게 간체자가 나갑니다. 지금은 전체 태그로 매칭하고
`hant` / `tw` / `hk` / `mo` 를 번체자로 보냅니다.

---

## 5. 히어로 영상

광화문 야경 9초. Canva 원본 `DAHTL4UsAYg`.

내려받은 원본이 1080p 11MB(9.5Mbps)라 그대로는 못 씁니다. 이렇게 줄였습니다:

```bash
ffmpeg -i 원본.mp4 -an -vf scale=1280:-2 -c:v libx264 -profile:v high \
  -pix_fmt yuv420p -crf 30 -preset slow -movflags +faststart assets/hero-720.mp4
ffmpeg -i 원본.mp4 -an -vf scale=854:-2  -c:v libx264 -profile:v main \
  -pix_fmt yuv420p -crf 32 -preset slow -movflags +faststart assets/hero-480.mp4
ffmpeg -i 원본.mp4 -vf "select=eq(n\,0),scale=1280:-2" -frames:v 1 -q:v 3 assets/hero-poster.jpg
```

결과: 720p 1.18MB / 480p 0.49MB / 포스터 0.14MB.
`-movflags +faststart` 는 빼지 마세요. 없으면 파일을 다 받아야 재생이 시작됩니다.
음성 트랙은 `-an` 으로 제거합니다 — 배경 영상이라 필요 없고 용량만 먹습니다.

**언제 내려받는가.** `preload="none"` 이라 페이지가 다 그려질 때까지
영상은 한 바이트도 받지 않습니다. 그 뒤 `app.js` 의 `heroVideo()` 가 판단합니다:

- `prefers-reduced-motion: reduce` → 받지 않음 (포스터로 끝)
- `navigator.connection.saveData` → 받지 않음
- `effectiveType` 이 2g 계열 → 받지 않음
- 화면 폭 768px 미만 → 480p, 그 이상 → 720p

셋 중 하나라도 걸리면 포스터 이미지가 그대로 히어로가 됩니다. 깨지지 않습니다.

**영상을 바꾸려면** 위 ffmpeg 명령으로 세 파일을 다시 만들어 `assets/` 에 덮어쓰고,
`sw.js` 의 `VERSION` 을 올리세요(캐시 무효화).

> 참고 — 지금 영상은 광화문입니다. 사이트의 첫 주장이 "관광이 아니라 산업"이라
> 논지와 살짝 부딪힙니다. 반도체 팹이나 야간 산업단지 영상을 확보하면
> 교체하는 편이 메시지에 맞습니다.

---

## 6. 로고

Canva 원본 `DAHTL5QRZjY` (1페이지가 로고, 2페이지는 빈 페이지).
원본은 먹색(#222)이라 **다크 배경 사이트에서는 보이지 않습니다.**
알파는 유지하고 RGB 만 치환해서 밝은 버전을 만들었습니다.

`assets/` 안:

| 파일 | 용도 |
|---|---|
| `logo-light.png` | 전체 로고(워드마크+태그라인), 밝은색, 배경 투명 — 푸터 |
| `logo-dark.png` | 같은 것의 어두운색 버전 — 흰 배경 문서·인쇄용 |
| `wordmark-light.png` / `wordmark-dark.png` | `ask` 만, 태그라인 없음 |
| `favicon-32/180/192/512.png` | 앰버 타일 + 먹색 `ask` |
| `icon-maskable-512.png` | 안드로이드 마스커블(안전영역 확보) |
| `logo-og.png` | 1200×630 링크 미리보기 카드 |

> ⚠️ **이름이 어긋나 있습니다.** 로고 태그라인은 **AI SUMMER IN KOREA** 인데
> 사이트 이름은 **AI Summer Korea** 입니다. 둘 중 하나로 통일해야 합니다.
> 대사관·재단에 보내는 자료에서 이름이 두 가지로 보이면 곤란합니다.

---

## 6-1. 섹션 밴드 이미지

긴 텍스트 구간을 끊어 주는 전면 이미지 두 장. 프로그램 뒤와 지원 안내 뒤에 있습니다.
둘 다 히어로와 같은 촬영본에서 구도만 달리해 잘라낸 것입니다 — 처마, 그리고 사람이 걷는 거리.

```
band-roof.jpg / .webp        1600w   처마    object-position: center 58%
band-roof-800.jpg / .webp     800w
band-street.jpg / .webp      1600w   거리    object-position: center 72%
band-street-800.jpg / .webp   800w
```

WebP 우선, JPEG 폴백, `loading="lazy"`, `width`/`height` 명시로 레이아웃 이동 없음.
전체 폭에서 WebP 47KB / 82KB.

**문구는 얹지 않습니다.** 문장을 하나 넣으면 8개 언어를 다 번역해야 하고, 그 문장이
낡으면 8곳을 고쳐야 합니다. 여기서 필요한 건 읽는 사람이 숨 쉴 곳이지 문장이 아닙니다.
`alt` 도 비워 두었습니다 — 장식이므로 스크린리더가 건너뜁니다.

밴드 위아래 가장자리는 그라디언트로 어둡게 깔아 페이지 배경과 이어지게 했습니다.
그게 없으면 잘라 붙인 사각형처럼 보입니다.

**사진을 더 확보하면** 같은 방식으로 늘리면 됩니다. 지금 두 컷은 한 장소(광화문)에서
나온 것이라, 서로 다른 장소 사진이 생기면 교체하는 편이 좋습니다. 특히 반도체 팹,
연구실, 캠퍼스 사진이 들어가면 사이트의 논지와 훨씬 잘 맞습니다.

> 캔바에서 가져오려면 해당 사진이 대표님 캔바 **디자인 안에** 있어야 합니다.
> 커넥터는 캔바 스톡 사진 라이브러리를 검색하지 못하고, 계정에 있는 디자인만 읽습니다.

---

## 7. PWA

- `manifest.webmanifest` — 이름, 아이콘, 테마색(`#0b0e13`), `display: standalone`
- `sw.js` — 두 가지 전략:
  - **HTML 과 `i18n.js` 는 네트워크 우선.** 문구·날짜·예산 숫자가 자주 바뀌므로
    캐시가 먼저 답해서 지난주 숫자를 보여주면 안 됩니다. 캐시는 오프라인 대비용입니다.
  - **`assets/`, `styles.css`, `app.js` 는 캐시 우선.** 크고 거의 안 바뀝니다.
- **미리 캐시하는 것이 없습니다.** 첫 방문자에게 1MB 영상을 강제로 받게 하는 건
  얻는 것보다 잃는 게 큽니다. 실제로 쓰인 자산만 캐시에 들어갑니다.

**정적 자산을 바꾸면 `sw.js` 의 `VERSION` 을 올리세요.** 안 올리면 재방문자는
계속 옛 파일을 봅니다. 문구만 고쳤다면 올릴 필요 없습니다(네트워크 우선이라 바로 반영).

---

## 8. 로컬에서 보기

빌드가 없으므로 정적 서버 아무거나면 됩니다.

```bash
npx --yes serve . -l 4399
```

서비스워커는 `localhost` 와 `https` 에서만 등록됩니다(`app.js` 에서 막아둠).
로컬에서 이상하게 캐시된다 싶으면 DevTools → Application → Service Workers → Unregister.

---

## 9. 아직 안 된 것 / 알고 있는 문제

**신청 폼이 없습니다.** `#applyBtn` `#volBtn` `#sponBtn` 세 버튼은 지금
`alert("Form coming soon / 접수 양식 준비 중입니다.")` 만 띄웁니다.
`app.js` 의 `wirePlaceholders()` 에 있습니다. 폼이나 메일 주소가 정해지면
이 함수를 지우고 `href` 를 실제 주소로 바꾸세요.

**자문위원 명단이 비어 있습니다.** 사이트가 "본인 동의를 받기 전에는 성함을
올리지 않습니다"라고 스스로 약속해 두었습니다. 동의 없이 채우지 마세요.

**ICOC 관련.** 소개 문단은 협회의 연원이 2019년 영국 ICOC 로 이어진다고
과거형으로 적혀 있습니다. 현재형("ICOC 의 한국 지부")으로 쓰지 마세요 —
ICOC LTD(영국 법인번호 11894741)는 2021-01-12 해산 상태이고,
등기 조회 한 번이면 드러납니다. 영국 법인을 되살린 뒤에는 현재형으로 바꿔도 됩니다.
상표(제40-2069695호)가 그 영국 법인 명의라는 점도 정리가 필요합니다.

**예산 수치는 잠정입니다.** 사이트에는 총액(21억 7천만 원 / 약 157만 달러 /
1인당 약 7,900달러)만 공개하고 항목별 내역과 자금 조달 계획은 후원 브리프로
넘기기로 했습니다. 간접비 비율과 현금·현물 배분은 협상 정보라 공개하지 않습니다.

**방문지·파트너는 전부 미확정입니다.** 상태 배지(확정/협의 중/계획)를 반드시
실제 상태와 맞추세요. 사이트가 "협약이 체결된 곳만 이름을 공개한다"고
써 두었으므로, 그 약속을 어기면 문서 전체의 신뢰가 무너집니다.

---

## 10. 고치기 전에 알아두면 좋은 것

- **CSS 는 단일 다크 테마입니다.** 라이트 모드 대응이 없습니다. 의도된 것이고,
  모든 색을 명시적으로 칠하므로 뷰어 테마와 무관하게 같게 보입니다.
- **표와 넓은 요소는 각자 `overflow-x:auto` 컨테이너 안에 있어야 합니다.**
  페이지 본문이 가로로 스크롤되면 모바일에서 바로 티가 납니다.
- **`build-static.js --check` 를 CI 에 걸어두세요.** `i18n.js` 와 `index.html` 의
  목록이 어긋나는 것이 이 저장소에서 가장 조용히 생기는 오류입니다.
